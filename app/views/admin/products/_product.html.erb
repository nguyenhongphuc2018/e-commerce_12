<tr>
  <td><%= product.id %></td>
  <td><%= product.name %></td>
  <td><%= product.price %></t>
  <td><%= product.category.name %></td>
  <td><%= product.descriptions %></t>
  <td><%= link_to t("admin.show"), admin_product_path(product) %></td>
  <td><%= link_to t("admin.edit"), edit_admin_product_path(product) %></td>
  <td><%= link_to t("admin.delete"), admin_product_path(id: product.id), method: :delete, data: { confirm: t("admin.products.index.you_sure") } %></td>
</tr>
