 <tr>
  <td><%= category.id %></td>
  <td><%= category.name %></td>
  <td><%= link_to t("admin.edit"), edit_admin_category_path(category) %></td>
  <td><%= link_to t("admin.delete"), admin_category_path(id: category.id), method: :delete, data: { confirm: t(".you_sure") } %></td>
</tr>
